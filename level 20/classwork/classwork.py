# Creating a list with 5 elements
my_list = [10, 20, 30, 40, 50]

# Slicing the list from the second element (index 1) to the last element
sliced_list = my_list[1:]

# Output the sliced list
print(sliced_list)
